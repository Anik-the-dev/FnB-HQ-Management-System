# # ---- Build stage ----
#     FROM node:20-alpine AS builder

#     WORKDIR /app
    
#     COPY package*.json ./
#     RUN npm install
    
#     COPY . .
    
#     # Build the React app (outputs to /app/dist)
#     RUN npm run build
    
#     # ---- Production stage ----
#     # Use lightweight nginx to serve the built React files
#     FROM nginx:alpine AS production
    
#     # Copy built files from builder stage into nginx's serve folder
#     COPY --from=builder /app/dist /usr/share/nginx/html
    
#     # Copy custom nginx config (handles React Router client-side routing)
#     COPY nginx.conf /etc/nginx/conf.d/default.conf
    
#     EXPOSE 3000
    
#     CMD ["nginx", "-g", "daemon off;"]